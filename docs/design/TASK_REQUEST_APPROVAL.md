# 設計書: タスク依頼・承認機能

## 概要

タスク作成時に承認フローを導入し、エージェント階層に基づいた権限管理を実現する。

### 背景

現在のタスク作成は以下の問題がある：

| 問題 | 説明 |
|------|------|
| 無秩序なタスク作成 | AIエージェントがチャット経由で無制限にタスクを作成可能 |
| 権限の不明確さ | 他人の下位エージェントにも自由にタスクを割り当て可能 |
| 監査の困難 | 誰が何を依頼したかの追跡が困難 |

### 目的

- エージェント階層に基づいたタスク作成権限の管理
- チャット経由のタスク作成を承認フローで制御
- 人間エージェントによるタスク承認・却下機能の提供

---

## 設計方針

| 観点 | 決定 | 理由 |
|------|------|------|
| ツール分離 | `create_task` と `request_task` を分離 | APIとして意図が明確 |
| AI公開範囲 | AIには `request_task` のみ公開 | 承認フローを強制 |
| 自動承認 | 上位→下位は自動承認 | 正当な権限者の利便性確保 |
| 承認権限 | 祖先すべてが承認可能 | 階層的な権限委譲に対応 |
| 通知 | システムチャットで通知 | 既存インフラ活用 |

---

## 権限モデル

### エージェント階層の例

```
Human A (Owner)
├── AI Worker A1
└── AI Worker A2

Human B (Owner)
├── AI Worker B1
└── AI Worker B2
```

### タスク作成権限マトリクス

| 作成者 | 担当者 | 関係 | 結果 |
|--------|--------|------|------|
| Human A | AI Worker A1 | 親→子 | 直接作成 or 自動承認 |
| Human A | AI Worker B1 | 他人の子 | 承認待ち → Human B が承認 |
| AI Worker A1 | AI Worker A2 | 兄弟 | 承認待ち → Human A が承認 |
| AI Worker B1 | AI Worker A1 | 他人 | 承認待ち → Human A が承認 |

### 承認権限

```
canApprove(approver, task) = isAncestorOf(approver, task.assignee)
```

- 直接の親: 承認可能
- 祖父母以上: 承認可能
- 兄弟・従兄弟: 承認不可
- 下位: 承認不可

---

## MCPツール設計

### ツール一覧

| ツール | 用途 | UI | AI (MCP) |
|--------|------|-----|----------|
| `create_task` | 通常のタスク作成 | ✓ | ✗ |
| `request_task` | タスク依頼作成 | ✓ | ✓ |
| `approve_task_request` | 依頼を承認 | ✓ | ✗ |
| `reject_task_request` | 依頼を却下 | ✓ | ✗ |

### request_task

```yaml
name: request_task
description: タスク依頼を作成する。依頼者が担当者の上位であれば自動承認される。

params:
  title:
    type: string
    required: true
    description: タスクのタイトル
  description:
    type: string
    required: false
    description: タスクの詳細説明
  assignee_id:
    type: string
    required: true
    description: タスクの担当者エージェントID
  priority:
    type: string
    enum: [low, medium, high, critical]
    default: medium
  due_date:
    type: string
    format: date
    required: false

returns:
  task_id: string
  approval_status: "approved" | "pending_approval"
  approvers: AgentID[]  # 承認可能なエージェント（pending時のみ）
```

### approve_task_request

```yaml
name: approve_task_request
description: タスク依頼を承認し、正式なタスクとして作成する。

params:
  task_id:
    type: string
    required: true

returns:
  task_id: string
  status: "backlog"
  message: string
```

### reject_task_request

```yaml
name: reject_task_request
description: タスク依頼を却下する。

params:
  task_id:
    type: string
    required: true
  reason:
    type: string
    required: false
    description: 却下理由

returns:
  task_id: string
  status: "rejected"
  message: string
```

---

## データモデル

### Task テーブル拡張

```sql
ALTER TABLE tasks ADD COLUMN requester_id TEXT REFERENCES agents(id);
ALTER TABLE tasks ADD COLUMN approval_status TEXT DEFAULT 'approved';
ALTER TABLE tasks ADD COLUMN rejected_reason TEXT;
ALTER TABLE tasks ADD COLUMN approved_by TEXT REFERENCES agents(id);
ALTER TABLE tasks ADD COLUMN approved_at TEXT;
```

| カラム | 型 | 説明 |
|--------|-----|------|
| requester_id | TEXT | 依頼者のエージェントID（直接作成時はNULL） |
| approval_status | TEXT | approved / pending_approval / rejected |
| rejected_reason | TEXT | 却下理由（rejected時のみ） |
| approved_by | TEXT | 承認者のエージェントID |
| approved_at | TEXT | 承認日時 |

### approval_status の状態遷移

```
                    ┌─────────────────┐
                    │ pending_approval │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
     ┌────────────────┐           ┌────────────────┐
     │    approved    │           │    rejected    │
     │   (= backlog)  │           │                │
     └────────────────┘           └────────────────┘
```

---

## フロー

### request_task フロー

```
request_task(title, assignee_id, ...)
        │
        ▼
┌───────────────────────────────┐
│ requester は assignee の祖先か？ │
└───────────────┬───────────────┘
                │
     ┌──────────┴──────────┐
     │ Yes                 │ No
     ▼                     ▼
┌─────────────┐     ┌──────────────────┐
│ 自動承認     │     │ pending_approval │
│ status=     │     │ で保存           │
│ approved    │     └────────┬─────────┘
│ → backlog   │              │
└─────────────┘              ▼
                    ┌──────────────────┐
                    │ 承認可能な祖先に   │
                    │ システムチャット   │
                    │ で通知            │
                    └──────────────────┘
```

### チャット経由のタスク依頼

```
Human B → AI Worker A1 (チャット):
「APIドキュメントを作成してください」
        │
        ▼
AI Worker A1 が解釈:
  requester = Human B (チャット送信者)
  assignee = AI Worker A1 (自分)
        │
        ▼
request_task(
  title: "APIドキュメント作成",
  assignee_id: "worker-a1",
  // requester は MCP セッションから取得
)
        │
        ▼
A1 は Human B の下位か？ → No
        │
        ▼
pending_approval で保存
Human A にシステムチャットで通知
```

---

## 通知設計

### システムチャットメッセージ

**承認依頼通知**
```json
{
  "type": "system",
  "subtype": "task_request",
  "content": "Human B から AI Worker A1 へのタスク依頼があります。",
  "metadata": {
    "task_id": "task-xxx",
    "title": "APIドキュメント作成",
    "requester_id": "human-b",
    "assignee_id": "worker-a1",
    "actions": ["approve", "reject"]
  }
}
```

**承認完了通知（依頼者へ）**
```json
{
  "type": "system",
  "subtype": "task_request_approved",
  "content": "タスク依頼「APIドキュメント作成」が承認されました。",
  "metadata": {
    "task_id": "task-xxx",
    "approved_by": "human-a"
  }
}
```

**却下通知（依頼者へ）**
```json
{
  "type": "system",
  "subtype": "task_request_rejected",
  "content": "タスク依頼「APIドキュメント作成」が却下されました。",
  "metadata": {
    "task_id": "task-xxx",
    "rejected_by": "human-a",
    "reason": "現在の優先度では対応できません"
  }
}
```

---

## Web UI 設計

### タスクボードでの表示

```
┌─────────────────────────────────────────────────────────┐
│  Backlog    │  To Do    │  In Progress  │ Done         │
├─────────────────────────────────────────────────────────┤
│ [Task 1]    │ [Task 3]  │               │              │
│ [Task 2]    │           │               │              │
│             │           │               │              │
│ ┌─────────────────┐     │               │              │
│ │ 🔔 Task 4       │     │               │              │
│ │ 承認待ち        │  ← 黄色/オレンジ背景               │
│ │ 依頼者: Human B │                                    │
│ │ [承認] [却下]   │                                    │
│ └─────────────────┘     │               │              │
│                         │               │              │
│ ┌─────────────────┐     │               │              │
│ │ ❌ Task 5       │     │               │              │
│ │ 却下済み        │  ← グレー背景、取消線              │
│ │ 理由: ...       │                                    │
│ └─────────────────┘     │               │              │
└─────────────────────────────────────────────────────────┘
```

### タスク作成UIの挙動

```typescript
async function handleCreateTask(taskData: TaskInput) {
  const isSubordinate = await checkIsSubordinate(
    currentUser.id,
    taskData.assigneeId
  )

  if (isSubordinate) {
    // 直接作成（create_task API）
    return await api.createTask(taskData)
  } else {
    // 依頼として作成（request_task API）
    return await api.requestTask(taskData)
  }
}
```

---

## REST API

### エンドポイント

| Method | Path | 説明 |
|--------|------|------|
| POST | `/api/tasks/request` | タスク依頼作成 |
| POST | `/api/tasks/{id}/approve` | 依頼承認 |
| POST | `/api/tasks/{id}/reject` | 依頼却下 |
| GET | `/api/tasks/pending` | 承認待ちタスク一覧 |

### POST /api/tasks/request

```json
// Request
{
  "title": "APIドキュメント作成",
  "description": "REST APIのドキュメントを作成",
  "assignee_id": "worker-a1",
  "priority": "medium"
}

// Response (自動承認時)
{
  "task_id": "task-xxx",
  "approval_status": "approved",
  "status": "backlog"
}

// Response (承認待ち時)
{
  "task_id": "task-xxx",
  "approval_status": "pending_approval",
  "approvers": ["human-a"]
}
```

### POST /api/tasks/{id}/approve

```json
// Response
{
  "task_id": "task-xxx",
  "approval_status": "approved",
  "status": "backlog",
  "approved_by": "human-a",
  "approved_at": "2026-01-24T10:00:00Z"
}
```

### POST /api/tasks/{id}/reject

```json
// Request
{
  "reason": "現在の優先度では対応できません"
}

// Response
{
  "task_id": "task-xxx",
  "approval_status": "rejected",
  "rejected_reason": "現在の優先度では対応できません"
}
```

---

## 実装計画

### Phase 1: 基盤実装

1. DBスキーマ拡張（approval_status, requester_id 等）
2. 階層判定ロジック実装（isAncestorOf）
3. `request_task` MCPツール実装
4. `approve_task_request` / `reject_task_request` MCPツール実装

### Phase 2: 通知・UI

5. システムチャット通知実装
6. REST API エンドポイント追加
7. タスクボードUI更新（承認待ち表示）
8. 承認/却下ボタン実装

### Phase 3: 統合・テスト

9. チャット経由タスク作成の統合
10. 統合テスト作成
11. ドキュメント更新

---

## 考慮事項

### 将来の拡張

| 拡張 | 説明 |
|------|------|
| 複数承認者 | 複数人の承認が必要なケース |
| 承認期限 | 期限切れで自動却下 |
| 委任 | 承認権限の一時的な委任 |
| 承認履歴 | 監査用の詳細ログ |

### エッジケース

| ケース | 対応 |
|--------|------|
| 承認者が複数同時に操作 | 先着順で処理、後続は「既に処理済み」エラー |
| 承認前にタスク削除 | 削除不可（却下を使用） |
| 依頼者の階層変更 | 承認時点の階層で判定（作成時の状態を保持） |
